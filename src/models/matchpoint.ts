export class matchPoint {

    public nick:string;
    public lat: string;
    public lng: string;
    public telefono: string;

    constructor(nick: string, lat:string, lng:string, telefono: string) {

        this.nick = nick;
        this.lat = lat;
        this.lng = lng;
        this.telefono = telefono;

    }

}